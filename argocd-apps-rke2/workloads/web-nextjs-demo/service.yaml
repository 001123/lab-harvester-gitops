apiVersion: v1
kind: Service
metadata:
  name: web-nextjs-demo
  namespace: web-nextjs-demo
  labels:
    app.kubernetes.io/name: web-nextjs-demo
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: web-nextjs-demo
