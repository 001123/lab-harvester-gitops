apiVersion: v1
kind: Service
metadata:
  name: web-nextjs
  namespace: web-nextjs
  labels:
    app.kubernetes.io/name: web-nextjs
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: web-nextjs
